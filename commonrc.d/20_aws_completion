if  [[ ! -z "$ZSH_NAME" ]] && hash aws_zsh_completer.sh 2>/dev/null ; then
    source "$(which aws_zsh_completer.sh)"
elif [[ ! -z "$BASH" ]] && hash aws_completer 2>/dev/null ; then
    complete -C aws_completer aws
fi
